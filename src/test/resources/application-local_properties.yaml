spring:
#  docker:
#    compose:
#      enabled: true
#      file: "src/test/resources/compose.yaml"
#      lifecycle-management: start_and_stop
  jpa:
    show-sql: ${SPRING_TEST_SHOW_SQL:false}
    hibernate:
      ddl-auto: ${SPRING_TEST_JPA_HIBERNATE_DDL_AUTO:create-drop}

## Custom Values for Variables
vars:
  db:
    schema: testdb
#    table-name: ${SPRING_APP_TEST_DATABASE_TABLE:prototype}

## Custom ConditionalOnProperty values
cop:
  database-service: ${SPRING_TEST_DATABASE_IMPLEMENTATION:jpa}
